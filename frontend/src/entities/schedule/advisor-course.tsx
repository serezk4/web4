import {ScheduleCourse} from "@/entities/schedule/types";

interface AdvisorCourseProps {
    course: ScheduleCourse;
}

export function AdvisorCourse({ course }: AdvisorCourseProps) {
    return (
        <div>AdvisorCourse</div>
    );
}